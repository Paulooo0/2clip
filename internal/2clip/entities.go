package clip

type Bucket struct {
	name string
}

type Database struct {
	name string
}

// TODO: Add create, delete and switch databases and buckets. Using a session to keep track of the current database and bucket.
